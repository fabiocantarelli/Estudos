let restoDeDivisao = (valorA, valorB) => {
    let divisao = valorA / valorB
    console.log(`O valor da divisão é ${divisao.toFixed(2)} e o resto é ${valorA % valorB}.`)
}


// % resto de divisão
// "/" Divisão

restoDeDivisao(10, 2)
restoDeDivisao(17, 3)
restoDeDivisao(1, 32)