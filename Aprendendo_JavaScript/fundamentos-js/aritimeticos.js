const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // pode ser usado para descobrir se um número é impar ou par, // 0 par // 1 impar//


// % resto de divisão

console.log(soma, subtracao, multiplicacao, divisao, modulo)